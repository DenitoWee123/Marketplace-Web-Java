--liquibase formatted sql

--changeset fmi:010-product-tags-and-stock-version
CREATE TABLE tags (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    name VARCHAR(50) NOT NULL UNIQUE
);
CREATE TABLE product_tags (
    product_id BIGINT NOT NULL REFERENCES products(id) ON DELETE CASCADE,
    tag_id BIGINT NOT NULL REFERENCES tags(id),
    PRIMARY KEY (product_id, tag_id)
);
CREATE INDEX idx_product_tags_tag ON product_tags(tag_id);
ALTER TABLE products ADD COLUMN version BIGINT DEFAULT 0 NOT NULL;
ALTER TABLE products ADD CONSTRAINT ck_products_quantity CHECK (quantity >= 0);

--rollback ALTER TABLE products DROP CONSTRAINT ck_products_quantity;
--rollback ALTER TABLE products DROP COLUMN version;
--rollback DROP TABLE product_tags;
--rollback DROP TABLE tags;
